<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.UserMapper">
        <insert id="insert" parameterType="com.sky.entity.User">
                <selectKey keyProperty="id" resultType="Long" order="AFTER">
                        SELECT LAST_INSERT_ID()
                </selectKey>
                insert into user (openid, name, phone, sex, id_number, avatar, create_time) values (
                #{openid}, #{name}, #{phone}, #{sex}, #{idNumber}, #{avatar}, #{createTime})
        </insert>

        <select id="countNewUsersByDateRange" resultType="com.sky.dto.UserStatisticsDTO">
                select
                DATE(create_time) as date,
                count(id) as newUserCount
                from
                user
                where
                create_time &gt;= #{beginTime} and create_time &lt;= #{endTime}
                group by
                DATE(create_time)
                order by
                date asc
        </select>
        <select id="countTotalUsersBeforeDate" resultType="java.lang.Integer">
                select count(id) from user where create_time &lt; #{beginTime}
        </select>
    <select id="countByMap" resultType="java.lang.Integer">
            select count(*)
            from user
            where create_time &lt; #{bengin}
    </select>
</mapper>
